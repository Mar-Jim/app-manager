# {{ name }}

Starter Databricks Asset Bundle dashboard skeleton.

## Targets

This project intentionally includes only the `{{ target }}` target. Staging and production deployments are handled by the external deployer repo.

Deployment strategy: `{{ deployment_strategy }}`

Dashboard deployment may need workspace-specific implementation.
